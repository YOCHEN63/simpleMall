<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oliver.simplemall.mapper.UserDao">
    <select id="selectByCondition" parameterType="com.oliver.simplemall.entity.UserDo" resultType="com.oliver.simplemall.entity.UserDo">
        SELECT * FROM user
        <if test="username != null">
            WHERE username = #{username}
        </if>
    </select>

    <select id="login" parameterType="String" resultType="com.oliver.simplemall.entity.UserDo">
        select * from user where username = #{username}
    </select>
    <insert id="insert" parameterType="com.oliver.simplemall.entity.UserDo">
        INSERT INTO user VALUES (#{id}, #{username}, #{password}, #{email}, #{gender}, #{phoneNumber}, #{userAuthority})
    </insert>

</mapper>